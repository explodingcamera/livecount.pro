export default override => {
	// const errorBackup = console.error;
	// console.error = (...args) => {
	// 	if (typeof args[0] === 'string' && args[0].includes(override)) {
	// 		return;
	// 	}
	// 	return errorBackup(...args);
	// };
};
